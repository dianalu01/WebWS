Cada vez que se modifique el WSDL
---------------------------------------

1) En la clase UsuarioModel
	
	a) agregar : import java.util.Date; 
		import javax.xml.bind.annotation.adapters.XmlJavaTypeAdapter;
	
	b) cambiar: 
		protected XMLGregorianCalendar fechaNacimiento;
		por
	    @XmlJavaTypeAdapter(DateAdapter.class)
	    protected Date fechaNacimiento;
	c)	cambiar: get y set de fechaNacimiento
	
1) En la clase AccesoModel
	
	a) agregar : import java.util.Date;
		import java.sql.Time;
		import javax.xml.bind.annotation.adapters.XmlJavaTypeAdapter;
	
	b) cambiar: 
		protected XMLGregorianCalendar horaacceso;
		por
	    @XmlJavaTypeAdapter(TimeAdapter.class)
	    protected Time
		protected XMLGregorianCalendar fecha;
		    por	        
	    @XmlJavaTypeAdapter(DateAdapter.class)
	    protected Date fecha;
	c)	cambiar: get y set de horaacceso
	
	
	
